<section
    id="{{content.glaucoma.condition}}"
    class="c-prompt"
    data-prompt
    aria-labelledby="title-{{content.glaucoma.condition}}"
    role="region">

    <div class="c-prompt__content">
        <h2
            id="title-glaucoma"
            class="c-prompt__title">
            {{{content.glaucoma.prompt}}}
        </h2>
        <p class="c-prompt__directions">
            {{{content.glaucoma.details}}}
        </p>
        <p class="c-prompt__example">
            This prompt helps you understand what it’s like to have a <a rel="external noopener" href="">{{content.glaucoma.type}} condition</a> like <a rel="external noopener" href="">{{content.glaucoma.condition}}</a>.
        </p>
    </div>
</section>
